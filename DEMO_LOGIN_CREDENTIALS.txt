==========================================================================
YASH ORNAMENTS - DEMO LOGIN CREDENTIALS
==========================================================================

*** IMPORTANT ***
THESE CREDENTIALS ARE FOR DEVELOPMENT AND STAGING ONLY.
DO NOT USE OR COMMIT REAL PRODUCTION CREDENTIALS OR SECRETS.
CURRENT MODE: ENVIRONMENT=production -> the demo OTP 1234 is DISABLED.
All OTPs are now real, randomly generated 4-digit codes sent via MSG91.
To re-enable the demo OTP for testing, set ENVIRONMENT=development in
backend/.env and restart the backend.

--------------------------------------------------------------------------
1. ADMIN LOGIN (Private admin portal at /admin/login)
--------------------------------------------------------------------------
   Phone : 9999813334
   OTP   : real SMS OTP via MSG91 (demo 1234 currently DISABLED)
   Role  : admin
   Notes : This is the approved administrator phone configured via the
           ADMIN_PHONES environment variable. In production a real OTP
           is sent to this phone via MSG91.

--------------------------------------------------------------------------
2. CUSTOMER LOGIN (Public enrollment website at /)
--------------------------------------------------------------------------
   Any valid 10-digit Indian mobile number can be used to enroll,
   for example:
   Phone : 9876512345
   OTP   : real SMS OTP via MSG91 (demo 1234 currently DISABLED)
   Notes : In production, a unique 4-digit OTP is sent by SMS via MSG91.
           After enrollment, the customer logs in to the Yash Trade App
           with the same phone number.

--------------------------------------------------------------------------
3. TELECALLER LOGIN (for testing 403 Forbidden on admin routes)
--------------------------------------------------------------------------
   Phone : 9999900001
   OTP   : real SMS OTP via MSG91 (demo 1234 currently DISABLED)
   Role  : telecaller
   Notes : This account can complete OTP login at /admin/login but
           receives "403 Forbidden" on every admin page and admin API,
           demonstrating role-based authorization.

--------------------------------------------------------------------------
4. DEVELOPMENT OTP
--------------------------------------------------------------------------
   OTP   : 1234
   Works : Only while ENVIRONMENT=development (or staging) in backend/.env
   Prod  : Set ENVIRONMENT=production to disable the demo OTP entirely.
           Real OTPs are then generated randomly (4 digits), stored
           hashed, expire in 5 minutes, and are delivered via MSG91.

--------------------------------------------------------------------------
5. SMS DELIVERY (MSG91)
--------------------------------------------------------------------------
   The MSG91 auth key is configured via MSG91_AUTHKEY in backend/.env
   (never committed to source control).
   NOTE: Real SMS delivery requires credit balance on the MSG91 account.
   As of setup, the account balance was 0 - top up in the MSG91
   dashboard to enable real OTP SMS delivery.

==========================================================================
