# YashTradeWeb — shared canonical integration

## Original product
Yash Ornaments two-step public customer enrollment with verified phone, Name, Shop Name and Location; genuine app download links; staff-only /admin for admin, telecaller and billing_executive. Branding and genuine business records must be preserved.

## Current requested stage (2026-09-11)
Implement the shared app contract in WEBSITE staging, pinned to YashTradeApp c4ee70d8134625a1a4e04073c43b9460c3c4e40d. Canonical backend owns identities/current permissions/customer/product/query/rate records and shared media. Website BFF owns opaque sessions, minimal drafts, outbox/cache only. No production changes, identities, storage migration or capacity purchases. Deliver code, tests, accurate parity/readiness/app handoff and private identity export tooling.

## Architecture implemented
- FastAPI `backend/server.py:create_app` with `backend/bff/{config,canonical,security,routes,proxy}.py`. All routes `/api`; fixed HTTPS upstream ending `/api`, no production fallback, no legacy SMS/token minting/local role seeding. Mongo via original MONGO_URL/DB_NAME; new bff_* collections only.
- Canonical portal OTP + /auth/me before website session. Secure HttpOnly __Host cookies, Origin + per-browser CSRF, Mongo CAS rotating-refresh lease, current-role validation, logout, bounded errors/media and route RBAC.
- Grant enrollment with server-bound stable payload/key and deliberate retry; Step 2 after canonical persistence. Canonical deletion grants, conservative cleanup/ack. Historical linkage cleanup blocks ack pending approved review.
- React canonical customer/staff/query/rate/product pages, virtualized catalog, private Blob photos, PDF author form and resumable reviewed import/crops, supporting lead/batch/banner/content/reward/media/settings pages.
- No website customer/product master or binary media library. No new passwords or reviewer access.

## Status
Website staging code implemented and built successfully. Final isolated suite:27passed (26backend +1multi-flow browser),0unexpected failures,0skipped,3strict XFAIL appdependenciesD2/D3/D4. Legacy unsafe helpers/unmounted placeholder screens removed; historical Mongo identities untouched. No production changes.
Operator configuration is NOT complete: CANONICAL_API_BASE_URL / ENROLLMENT_INTEGRATION_KEY / STAFF_SERVICE_KEY / BUILD_COMMIT missing. App preview STAFF_SERVICE_KEY absent. No real canonical login/SMS/storage write test performed.
Read-only health shows app production shared-v1-followup-2026-09-11, commit unrecorded (changed outside this task from earlier v7 observation); both website production domains remain login-v10. Pinned116-path OpenAPI matched apppreview initially and production at finalcheck; apppreview later returned404. No alternate production target was adopted. Matching schema is not source/DB isolation proof.

## Completed in this stage
- End-to-end grant enrollment, canonical-only role/session auth, CSRF/exactorigin/security/refresh and currentrole checks. Origin issue: previewingress rewritesOrigin to exact clusteralias; explicitly pinned with customwebsiteorigin +CSRF checks, never a wildcard/missingorigin exception. TrustedclientIP remains operatorgate.
- Canonical customer pages/filter/details/assignment, staff directory/conversion, all7enquiry types with per-role queues and metrics, rates/slabs/audit/recenthistory, virtualizedcatalog/privatephoto uploads. LeadCRM/summary, batchselection/softdelete, banners/content, rewards/billing and media/privacy views.
- Actual browser Filebytes→canonical photo upload and reviewedPDF lifecycle,4,064,397-byte sample/fourpages/threeproducts includingtwo ononepage/allthree metals, title+visualcrop edits, hiddencommit created3. Formauthoring exports privatePDF withoutcreatingproducts; sample+companionJSON downloads. Browserresponsive checks320/768/1024/1440.
- Read-only identity export tooling with strict synthetic-testedprojection and restrictedoutput. Actual genuineexport NOTRUN. Countmanifest customers0/staff2/deletionreferences2, zero bffsession/draft/cache/outboxrecords; no canonicalIDs inferred. Reconciliation remains unexecuted/DRYRUN until exactapproval andbackup.
- Deliverables: root WEBSITE_IMPLEMENTATION_REPORT.md, ADMIN_PARITY_CHECKLIST.md, WEBSITE_RELEASE_READINESS.md, APP_TEAM_HANDOFF.md; canonicalreference docs updated; evidence/shared-v1/final-junit.xml, final-pytest.log, FINAL_VALIDATION_REPORT.json and screenshots. Tests/shared_v1/README.md reproducibility, pytest.ini excludes unsafelegacytestdiscovery.
- Localbranchmain and GitHubmain both still bf7808e27129de2b6ec8a5d521c1705f44fa7725 at verification; this is baseline NOT implementationSHA. Newcode not yet GitHubsynced; BUILD_COMMITunrecorded. No gitwrites done.

## Priorities
- P0: Code review and exactwebsitecommit/sync/buildprovenance. AppdependencyD4: deletionoutboxstarvespastfirst100websiteacknowledgements (critical). D2staticcustomers/searchshadowing; D3immutablecustomerIDqueryfilterabsent. All three reproducedstrict XFAIL; do not altercanonicalpin silently. D1targetedverifiedphonecorrectionunsupported, keptread-only. LegacyhardproductDELETE not exposed; safeunpublish retained.
- P1: Confirm approved staging canonical target/isolation; provision distinct matching two-sided private keys and trusted ingress; real controlled OTP/SMS/storage acceptance. Read-only private export transfer + tested restorable backup + exact hash/per-ID reconciliation approval. Store release URLs verification.
- P2: Exhaustive dateboundary/metricwinner/drilldown/continuity/auxiliaryCRUD/PDFpartial-duplicate-publication-owner-restart permutations; native Android/iOS, provider erasure/retention, full-service load/chaos/capacity and private review access. Only then separately approved coordinatedcutover/admincontinuity/revocation/rollbackchecks.
- Release classification: ready for website CODE REVIEW with disclosed gaps, NOT ready for live controlled cross-system testing or separately approved cutover.
- Final verification: default root `pytest -q` (restricted to isolated shared_v1) passed after all code/config changes; final counts remain27passed/3strictXFAIL. `yarn build` compiled successfully without warnings; Pythoncompile passed. Real canonical portal login remains unverified and GitHub main remains baseline at the last read-only check. Final user response must distinguish these from successful isolated tests.
- Future: enrollment notifications and missing-rate reminders after core acceptance. Never reintroduce customer Live Bhav or AI Try-On.

## Production authentication incident — current request
User reports screenshot on register.yashsilver.com showing 'An approved canonical staging API is not configured', asks RCA/fix across logins. User APPROVES private production configuration correction IF accessible; all verification remains isolated/no real SMS.

Confirmed both production domains now run staging shared-v1 build with canonical base + both website keys missing. Canonical app staff key also missing. Existing website LIVE_INTEGRATION_KEY matches app enrollment key via safe GET invalid-phone422 (no person lookup/SMS). Customdomain invalid-body probe422; hostedwebsite403ORIGIN_REJECTED. Oldhealth incorrectlyHTTP200statusok despite notready. Production promotion provenance unavailable; do not blame user or claim a deployment action was performed here.

Preparing auth-readiness-v1 correction: per-flow readiness and correct503health/200liveness, neutral availability UI/originprecheck, no local auth fallback, zeroSMS readiness command. Existing role/session security retained. Production private-settings writer for BOTH systems is unavailable in this workspace; no actualproductionconfigmodified and localpreview remains disconnected. Do NOT commit .env despite scanner suggestion. New docs PRODUCTION_AUTH_RCA.md and PRODUCTION_AUTH_CONFIGURATION.md list exact two-sided actions. Mandatory testing_agent verification pending before concluding; do not claim production restored.

Incident final verification: testing_agent iteration11 reports33passed and3knownstrictXFAIL (D2/D3/D4); new missingconfig/perflow/origin/readiness GET-only tests plus existing full authenticatedbrowserregressions. Neutral previewblockedstates verified with0OTPmutationposts. No realSMS/genuineidentitychanges. Staticdeploymentscan subsequentlypassed structuralchecks but doesnotproveproductionready. Productionstilloldbuild/newreadyendpoint404 and settingsunprovisioned. NextP0: authorizedoperator applies canonical appSTAFFkey +matchingwebsiteSTAFFkey, explicitproductioncanonicalURL, deliberateexistingvalidenrollmentkeymigration andbothproductionorigins (productionaliasesonlyifverified). Then read-onlyhealth/originchecks; keeprealOTPverificationoutofscope. This is a confirmedRCA and testedcodeguardrailfix, NOT a completedproductionrestoration.

## Readiness adapter v2 — 2026-09-12 (this stage, DONE in code; production still blocked)
User choices: 1A pin app 6a6cddd, 2A shared credential-verified snapshot for /api/health/ready + /api/public/auth-status + ensure_flow, 3A tracked evidence copies, 4A single PRODUCTION_OPERATOR_HANDOFF.md.
- App contract pin `/root/yash-contract` = 6a6cdddb81a4c27b387144746a7b6cf7fefc85c2 (build shared-v1-owner-recovery-2026-09-12, 120 OpenAPI paths, capabilities.credential_readiness=1). Old c4ee70d copy kept at `/root/yash-contract-c4ee70d`. `CONTRACT_COMMIT` updated; website `BUILD='website-shared-v1-readiness-adapter-v2'`.
- `bff/canonical.py`: `PROBES` exact allowlist {/health:None, /integrations/staff/readiness:'staff', /integrations/enrollment/readiness:'enrollment'}; `headers(..., method)` scope check is method-bound; `Canonical.probe(path,key)` bounded GET (5s/8s), no redirects, returns (status, dict) only for JSON 200/401/503.
- `bff/readiness.py`: `parse_health` accepts documented AuthReadiness 200/503 (per-flow issues → `CANONICAL.<NAME>`), legacy public-health 200-only fallback (`upstream.contract='public_health'`, credential_verified null); `parse_credential` → 401 = `CANONICAL_CREDENTIAL_MISMATCH`, 503 SharedError = `CANONICAL.<code>`; anything undocumented = `CANONICAL_UNAVAILABLE_OR_INCOMPATIBLE`. Credentials only travel for flows with zero issues; one probe per secret; cache 30s/5s with lock coalescing; `invalidate()`; `clock` injectable. Report adds `key_matching_verified_by_this_check`, `sms_delivery_verified=false`, `account_role_verified=false`.
- Tests: new `tests/shared_v1/test_bff_readiness_adapter.py` (25, MockTransport, real Canonical/Readiness); updated StubCanonical. Full suite 61 collected = 58 passed + 3 strict XFAIL (D2/D3/D4 re-verified still present at 6a6cddd). testing_agent iteration_12: same counts, no issues. Evidence (sanitized) tracked under `evidence/auth-production-incident/` incl. copied iteration_11 report + JUnits, `readiness-adapter-v2/{junit.xml,pytest.log,RUN_SUMMARY.json,iteration_12.json,pytest_iteration12_full.xml}`. `test_reports/` stays gitignored.
- Live read-only 2026-09-12: app /health/live 200; /health 503 staff-only (STAFF_SERVICE_KEY); both websites /health/ready 503 missing all three settings on old build auth-readiness-v1; register.yashsilver.com origin ok, yash-register.emergent.host origin_allowed=false (settings: BFF_ALLOWED_ORIGINS lists one origin).
- NOT done / blocked: production settings (no writer here), app STAFF_SERVICE_KEY, owner role repair (operator command `backend/tools/recover_owner_admin.py` at app 6a6cddd), genuine OTP login. Single next action = authorized operator executes PRODUCTION_OPERATOR_HANDOFF.md Steps 1–3, then owner does one approved OTP login.
- Still pending from earlier: placeholder store URLs gated by ANDROID/IOS_RELEASE_VERIFIED (P1); D2/D3/D4 consumption when app publishes fixes (replace strict XFAILs with passing tests; update allowlists/UI).

## D2/D3/D4 consumers + owner-executed production prep — 2026-09-12 (v3, DONE in code; production unchanged)
Settled decisions from the owner's execution prompt (no further preference rounds):
- Owner is the production operator via Manage Publishes → Secrets/Database + normal Re-publish; new key NAMES are declared by the agent in the actual backend/.env (gitignored) with non-working placeholders; existing production values change only in Secrets; never "Replace with a fresh database"; never ask for another operator or for secret values in chat; controlled real OTP to 9999813334 is authorized (one login per origin, ≤1 resend); app chat owns owner-role recovery (ID bcdf18c9-dc87-4d46-b580-30cf519103df) and D2/D3/D4 app fixes; website never gets local role/reviewer auth.
- Actual `backend/.env` now: BFF_ALLOWED_ORIGINS=both production origins; CANONICAL_API_BASE_URL=https://yash-tryon-test.emergent.host/api; ENROLLMENT_INTEGRATION_KEY / STAFF_SERVICE_KEY / BUILD_COMMIT = SET_IN_PUBLISH_SECRETS (no valid private value existed under those names; LIVE_INTEGRATION_KEY untouched). Preview therefore probes production public /health only (no credentials leave; flows unready by name) and rejects the preview origin (expected).
- `bff/config.py`: PLACEHOLDER_MARKERS mirror app core (SET_IN_PUBLISH_SECRETS, PLACEHOLDER, REPLACE_ME, CHANGE_ME, UNCONFIGURED); `setting()` + `__post_init__` reject placeholders on every construction path; list settings filtered per entry; runtime env wins over .env (tested). BUILD='website-shared-v1-d2d3d4-consumers-v3', CONTRACT_COMMIT=9596a5578a61bb1fb187e63345b7f93eda95bc9c. `/root/yash-contract` = app 9596a55 (older copies: /root/yash-contract-6a6cddd, /root/yash-contract-c4ee70d). `contracts/openapi.shared-v1.json` = 123-path schema (sha256 a30e6b2f…, byte-identical to ZIP). Whole-source provenance of the app ZIP NOT verified.
- D2: proxy RULES order fixed (BFF itself shadowed `customers/search` with `customers/{ID}` → 403 for billing); rule `('GET','customers/search',B)` precedes the dynamic rule. Rewards page gained `CustomerSearch` (data-testids reward-customer-search, reward-search-pick-<id>, reward-search-scope) honoring minimum_length/limit envelope.
- D3: UserDetail gained `CompleteHistory` (paginated `/requests?customer_id=` with status/type/sort; testids customer-complete-history-*); proxy withholds `requests?customer_id=` with 503 CAPABILITY_UNAVAILABLE unless upstream capability `customer_id_history` (readiness snapshot `upstream.capabilities`). Recent-100 profile array kept, labelled.
- D4: `consume_deletions` paginates limit/after/next_cursor (≤50 pages), cleans website collections per event, acks one event at a time (idempotent upstream), counts ack_failed_retry_later, tolerates 404, reports cursor_feed capability; crash/retry/idempotency/no-resurrection tested with 150/237 events.
- `tools/check_auth_readiness.py` is now a strict release gate: requires EXPECTED_WEBSITE_BUILD/COMMIT(hex)/APP_CONTRACT_COMMIT/UPSTREAM_BUILD/UPSTREAM_COMMIT + PUBLICATION_RECEIPT, HTTP 200 JSON, integration_ready, credential_readiness contract, key_matching true, each flow ready+credential_verified+no issues, origin_allowed + public flows available on EVERY origin; legacy/null/false/malformed/contradictory → exit 1. 34 tests in test_release_gate_tool.py.
- Suite: 104 passed, 0 skipped, 0 xfail (former strict XFAILs replaced by genuine passing D2/D3/D4 consumer tests + browser UI steps). Evidence: evidence/d2d3d4-consumers-v3/{junit.xml,pytest.log,production-readonly-2026-09-12.json}.
- Live read-only 12 Sep (latest): app production still shared-v1-owner-recovery-2026-09-12 (120 paths, STAFF_SERVICE_KEY missing, no customer_id_history/deletion_outbox_cursor caps → saved app 9596a55 not deployed); BOTH website domains now run website-shared-v1-readiness-adapter-v2 (pin 6a6cddd) with settings still missing/placeholder; hosted alias origin_allowed=false. SMS requested NO / received n/a / login verified NO (prerequisites unmet).
- Git: GitHub main c1ef7d84609e0d0eff0258601b2fc1a84076189e at audit; local platform commits newer; owner must Save to GitHub then set BUILD_COMMIT.
- 2026-09-12 later: both domains now run v3 and app runs 9596a55 (review-fonts) but ALL five website keys exist in production Secrets with placeholder values (Secrets override .env; support email confirms existing keys must be edited in Secrets) and app STAFF_SERVICE_KEY still missing. Added `configuration_state` (valid|placeholder|missing|invalid by name) to /api/health/ready; Settings.__post_init__ records placeholders. Owner must edit values in Secrets on both projects, then republish.
- 2026-09-12 latest live: website Secrets now valid for CANONICAL_API_BASE_URL/ENROLLMENT_INTEGRATION_KEY/STAFF_SERVICE_KEY; app STAFF_SERVICE_KEY configured (app build review-fonts). ONLY blocker: website BFF_ALLOWED_ORIGINS Secret has a malformed entry (list fails closed; hosted alias unmatched). Added `origin_entries` (index/valid/reason/host, no raw echo) to /api/health/ready. App-side observations for the app chat: OTP_DEMO_MODE=true with OTP_DEMO_PHONES in production Secrets (security flag), REVIEW_DB_NAME placeholder (optional). Website BUILD_COMMIT still placeholder (non-blocking for readiness, required by release gate).
- Test hygiene: fixed IST-vs-container-date bug in test_request_resolution_reopen (period metrics failed only 18:30–24:00 UTC). Observed twice an unexplained intermittent USER_NOT_FOUND for telecaller 9000000102 login in followup tests (not reproducible in 3 later runs; all fixtures function-scoped) — watch. Playwright Chromium at /pw-browsers is lost on pod restart; reinstall with `python -m playwright install chromium` before the browser test. Full suite after fixes: 106 passed.
- Pending owner/app: PRODUCTION_OPERATOR_HANDOFF.md ordered checklist §2–§5. Pending website: re-pin when app publishes its packaging correction; store links when published.

## Owner-admin release adoption — 2026-09-12 (v4, DONE in code; tested 108/108; production republish pending)
User prompt: fetch app handoff at commit "Default owner administrator: OWNER_ADMIN_PHONE=9999813334 …" = `a0b1e8085ba6ac679fa0f5ec4e106d928057ed8f`, verify sha256 vs HANDOFF_MANIFEST.json, execute WEBSITE_AUTH_FIX_PROMPT.md steps 0–7, adopt a0b1e80 as pin, mocked tests only, never real OTP, report names/status only.
- Integrity: the ZIP inside a0b1e80 is the PREVIOUS package (manifest source 84a11f9 → hashes don't describe a0b1e80). App packaging model puts the rebuilt ZIP in the next commit `9e5aa3e` (manifest source_commit=a0b1e80): all 8 file sha256 + git blob ids match the raw a0b1e80 files; whole-tree digest 6834fad2… over 394 blobs matches. Schema a30e6b2f… (123 paths) byte-identical to the 9596a55 pin → `contracts/openapi.shared-v1.json` unchanged. Flagged to user.
- App-side facts observed live (12 Sep, read-only): production app already runs a0b1e80 code — `capabilities.owner_admin_bootstrap=1`, `flows.owner_admin={ready:true,state:already_admin|promoted,phone_suffix:"3334"}`, `configuration.OWNER_ADMIN_PHONE=true`, STAFF_SERVICE_KEY true, BUILD_COMMIT false. Owner record is admin canonically; website needs no role work.
- Step 0: backend/.env already declared exactly as required (CANONICAL_API_BASE_URL, ENROLLMENT_INTEGRATION_KEY=SET_IN_PUBLISH_SECRETS, STAFF_SERVICE_KEY=SET_IN_PUBLISH_SECRETS, BFF_ALLOWED_ORIGINS both origins, BUILD_COMMIT placeholder) — no change. Steps 1–6 were already implemented (portal channel, X-Staff-Service-Key, 503 states, /health/live, credential probes, /auth/me routing, STAFF_ONLY denial) and are verified live: both production domains `/api/health/ready` 200, all flows credential_verified, key_matching true, both origins valid; only BUILD_COMMIT placeholder.
- Step 7 (pin): `/root/yash-contract` = a0b1e80 checkout (old copy `/root/yash-contract-9596a55`); config BUILD='website-shared-v1-owner-admin-pin-v4', CONTRACT_COMMIT=a0b1e80; readiness CAPABILITIES += owner_admin_bootstrap; `parse_owner_admin()` → `upstream.owner_admin {reported, ready, issues CANONICAL.*, state∈{created,promoted,already_admin}|null, phone_suffix 4 digits|null}` (read-only, never gates website flows, never an ID). Settings & Privacy page: "Canonical app connection" panel (testids canonical-connection, upstream-build, upstream-capabilities-*, flow-readiness-*, owner-admin-state, owner-admin-boundary). Tests: +MockTransport owner_admin test, +real pinned-app test (synthetic 9000000106 promoted by app bootstrap → website login role admin same id, admin route 200, no website-local collection, customer STAFF_ONLY), browser journey Settings step; pin refs updated in tests/README/release-gate constants. Suite 108 passed / 0 failed / 0 skipped (evidence/owner-admin-pin-v4/{junit.xml,pytest.log,RUN_SUMMARY.json,production-readonly-2026-09-12.json}). testing_agent iteration_14: all backend checks pass; noted one non-reproducible flake in test_request_resolution_reopen_idempotency_history_and_today_metrics_cohort (pre-existing; passes on rerun).
- PRODUCTION_OPERATOR_HANDOFF.md rewritten v4 (pin a0b1e80, live status, owner-admin bootstrap, no manual role recovery, correct canonical ID in §4.4).
- Not done here / owner: website Republish (domains still serve v3), website BUILD_COMMIT Secret (Save to GitHub → SHA → Secrets → Republish), genuine owner OTP login per domain, app BUILD_COMMIT Secret. Placeholder store URLs still pending (P2).

## Win-back, churn insight, returning recognition — 2026-09-13 (v5, DONE in code; tested)
User asked to keep a website copy of customers after account deletion for re-marketing. REFUSED as stated (Google Play deletion policy + DPDP apply to the same business; would falsify the D4 ack). User accepted the compliant alternatives 1–4 and asked to implement all:
1. **Win-back opt-in at deletion** — `/delete/confirm` body gained `winback_consent` (default false) + optional `reason` (REASONS: not_buying/other_supplier/app_problems/too_many_messages/privacy/other). Canonical profile is read once before erasure (GET /integrations/customers/{phone}, enrollment key); if consent ticked → `bff_winback_contacts` {phone,name,shop_name,location,source=deletion_optin,status=new,reason,consent_version=winback-offers-v1,consented_at,history,expires_at=+365d TTL}. Account is still deleted and D4 ack stays truthful. D4 `cleanup_event` deliberately never touches winback/churn collections.
2. **Pre-deletion callback** — `POST /api/delete/callback` (Verify + note≤300): same deletion OTP proves ownership, account KEPT, contact stored source=pre_deletion_callback status=callback_requested; deletion draft consumed (fresh OTP needed to delete later). Canonical POST /requests needs a customer bearer, so the callback is website-local only.
3. **Anonymous churn** — `/delete/confirm` stages detail (registered_month, location title-cased, reason, source=website) in `bff_churn_pending` keyed by digest(reference), TTL 30d; `winback.record_churn` runs inside D4 `cleanup_event` for EVERY erasure event (website or in-app) → `bff_churn` row {deleted_month, registered_month, location, reason, source} — no id/phone/name; idempotent via `bff_churn_marks` (digest('churn:'+event id), TTL 90d). In-app deletions → source=app, no detail.
4. **Deleted-number recognition** — `bff_deleted_numbers` {_id=HMAC(sha256('winback-number-hash:'+SESSION_SECRET), phone), deleted_at, expires_at +730d}: written at /delete/confirm and, for in-app deletions, from the website's own completed enrollment draft (phone) before the draft is erased. `persist_enrollment` → `note_returning` writes `bff_returning` {_id=canonical id, enrolled_at, previously_deleted_at} (cleaned by D4 on later deletion). LIMITATION: the canonical app tombstones deleted numbers forever (`deleted_identities` → DELETED_IDENTITY on enrollment, login blocked), so real returning registrations only happen if the app team relaxes that; the plumbing + UI note are in place.
- Staff API (`bff/winback.py`, staff_session, roles admin+telecaller; billing → 403): GET /api/admin/winback/contacts?status&source&page&limit; PATCH /contacts/{id} {action: contacted|converted|reopen, note}; POST /contacts/{id}/opt-out (erases phone/name/shop/location/note, status opted_out, further edits 404); GET /churn?months (by_month website/app, by_location top15, by_reason, winback counts, returning count); GET /returning; POST /match {phones≤100} → {phone: deleted_at} (hashed in memory).
- Frontend: DeleteAccount.js (reason select, unticked consent checkbox, "Talk to us first" toggle + note, callback-result, deletion-winback-kept); new `pages/admin/Winback.js` at /admin/winback (nav "Win-back & Churn" admin+telecaller; contacts table + editor with Contacted/Converted/Reopen/Opt-out + history; churn tables; returning table); Users.js "Returning" badge via /match; Privacy.js retention rows for win-back contacts, deleted-number hash, churn stats. server.py: TTL indexes for bff_winback_contacts/bff_deleted_numbers/bff_churn_pending/bff_churn_marks.
- Tests: `tests/shared_v1/test_bff_winback.py` (6: opt-in, no-opt-in, callback keeps account, staff RBAC/actions/opt-out/churn/match, returning flag + in-app deletion clears it, in-app deletion hash from draft); browser journey gained deletion opt-in + callback + telecaller Win-back console steps (screens public-deletion-winback-1440.jpeg, admin-winback-1440.jpeg). Synthetic numbers 90000001{31..39}. Evidence: evidence/winback-v5/.

## PDF import "Upload & analyze disabled" — 2026-09-13 (bug fix, tested iteration_16)
- Owner (Aditya, role admin on production — the live login works) chose `Silver_Upload_01.pdf` but the button stayed disabled. Root cause: button requires a selected batch; the page gave no reason and no way to create a batch. Fix in `pages/admin/PdfImport.js`: `pdf-upload-blocker` explanation (batch / PDF / type / size / limits-or-batch-list load failure), inline "Or create a new batch for this import" (`pdf-new-batch` + `pdf-create-batch` → POST /bff/batches, reload, auto-select), `pdf-file-summary` (name · MiB · chunks), `pdf-batches` State/refresh, type/size validation before click. Browser journey reproduces the reported state and the inline batch creation.
- The user's PDF was validated with the pinned app parser (`/root/yash-contract/backend/shared/pdf_parser.py`): 152 pages (2 GUIDE), 300 products YS-PS01-001…YS-PS04-100, all unique, 0 errors, 24.5 MB / 25 chunks, within 64 MiB / 200 pages; every photo 800×800 → app warning "Low-resolution photograph" (< 1024 px) on all 300 rows — import still allowed. Copy kept at evidence/pdf-import-silver01/. Playwright chromium must be reinstalled after pod restarts (`python -m playwright install chromium`).

## PDF import RENDER_TIMEOUT mid-analysis — 2026-09-13 (website auto-retry, tested)
- Production run: upload completed (24 chunks), analysis processed 19/152 pages (36 products) then `phase=error`, `RENDER_TIMEOUT: page exceeds the resource budget`. Source: app `shared/pdf_jobs.run_parser` — one subprocess per page, `asyncio.wait_for(..., page_timeout_seconds=25)`, worker RLIMIT_CPU 22 s / RLIMIT_AS 512 MiB; job checkpoints `next_page` and `POST /pdf-upload/{jid}/resume` re-queues from it. Same PDF here: ~0.6 s/page → the production app server is CPU-starved/contended, not the file.
- Website fix `bff/pdf_watch.py` (`ImportWatch`, app.state.pdf_watch, collection `bff_pdf_watch` TTL 7d): armed by the proxy on POST complete/resume, stood down on pause/cancel/commit. Polls canonical status every 10 s with the owner's staff session (`bff_sessions` row by sid; rotates token like staff_session; never bumps last_active); on transient errors (`RENDER_TIMEOUT:`, `RENDER_RESOURCE_LIMIT:`, `IMPORT_PROCESSING_FAILED:`) waits backoff(15 s×attempt ≤60 s) and POSTs resume; per-page attempts reset when `pages_processed` advances; stops with reason: finished / paused_by_operator / cancelled_by_operator / stopped_by_operator / upload_incomplete / needs_correction (PDF defect, never retried) / page_retry_exhausted (5 per page) / resume_budget_exhausted (400) / time_budget_exhausted (6 h) / session_ended / access_ended / app_unreachable (12 consecutive failures). `restore()` re-spawns active watchers at startup. Endpoints: GET /api/admin/pdf-watch/{jid}, POST /api/admin/pdf-watch/{jid}/stop (admin, owner-scoped, 404 WATCH_NOT_FOUND).
- Frontend PdfImport: polls watch + status while queued/analyzing or (error & watch active); `pdf-watch` line explains ON state / each stop reason; Resume analysis restarts the watcher via the proxy hook.
- Tests `tests/shared_v1/test_bff_pdf_watch.py` (4): timeout → resume → review (3 rows, no duplicates); per-page exhaustion → operator resume restarts → pause stands down → stop endpoint; BOUNDARY_ERROR never retried + owner/admin scoping (billing 403, unknown 404); session deletion → session_ended. Suite 118 tests.
- APP-SIDE ASK (cannot be fixed on the website): raise `page_timeout_seconds` (25 → ~90) and/or CPU for the app container; with the current budget a page that consistently needs > 25 s will fail 5× and the watcher gives up (message tells the owner to retry later or ask the app team).

## PDF import: completion feedback + multi-file queue — 2026-09-13 (v6, tested)
User: "upload finished but no green tick", "second upload shows Resume instead of Upload", "can multiple files queue?". Causes: no explicit success banner; page locked to ONE localStorage resume record (`yash-pdf-resume-v1-<uid>`) until commit/cancel + "Start another import".
- New `lib/pdfImport.js` (loadJobs/saveJobs list `yash-pdf-imports-v2-<uid>` with one-time migration of the v1 record; `transferFile` = validate → sha256 → init → resume-if-paused → missing chunks → complete; `fileProblem`; ACTIVE/DONE phases; MAX_ACTIVE=4 = canonical `ACTIVE_IMPORT_LIMIT`), new `components/admin/ImportJob.js` (per-job card: status polling incl. 3 s while working, colour banner with icon — green ✓ for queued/review/committed, spinner analysing, amber for transfer-incomplete/error, grey paused/cancelled/expired — metrics, auto-retry text only while queued/analyzing/error, Pause/Resume/Cancel/Review & commit/Show outcome/Remove; testids suffixed `-<jid>`), rewritten `pages/admin/PdfImport.js` (multi-select file input; sequential queue runner: waits with `pdf-queue-waiting` when 4 slots are in use or init returns 429 ACTIVE_IMPORT_LIMIT and continues automatically when a slot frees; "Stop after current file"; per-file progress lines; first job reaching review auto-opens its review; jobs heading shows slots). Canonical job id is deterministic digest(user:batch:sha:mode) so re-selecting a file resumes/idempotently re-inits.
- Constraint (app): max 4 imports open (uploading/queued/analyzing/review/paused) per admin — queue beyond 4 waits for a commit/cancel; selected files persist only while the tab is open (File objects cannot be stored).
- Browser journey now uploads 2 PDFs in one queue (second = sample + trailing comment bytes), asserts both reach review with green banners, "2 of 4 slots in use", then commits the auto-opened review (3 products). Screens: evidence/shared-v1/pdf-queue-two-files-1440.jpeg.

## PDF import: upload progress bars + unmistakable batch step — 2026-09-13 (v7, tested)
User: "When uploading, the upload button just disables. There should be a progress bar." Screenshot actually showed the pre-upload state with NO batch chosen (blocker text was small and missed).
- `lib/pdfImport.js` `transferFile` now reports (text, bytes) per chunk incl. already-acknowledged chunks on resume, 'Finishing upload…' before /complete, and calls `onInit(record)` right after init so the job card appears while uploading.
- `components/admin/ImportJob.js`: exported `ProgressBar` (role=progressbar, aria-valuenow, `<id>-fill`); card banner shows a bar during transfer (`pdf-job-progress-<jid>`).
- `pages/admin/PdfImport.js`: live queue panel while running (`pdf-queue-progress`, `-text` "Uploading file k of N: name · status", `-percent` overall % and MiB, `-bar`), per-file bars (`pdf-file-progress-<i>`), button reads "Transferring…"; batch requirement made prominent: batch row ring + "Batch — required" label when files are chosen without a batch (`pdf-batch-row`), blocker rendered as an amber alert box with icon and step wording; last-used batch remembered per admin (`yash-pdf-last-batch-<uid>`) and preselected if it still exists; a single existing batch is auto-selected.
- Browser journey: seeds a 2nd batch (so nothing auto-selects) to keep the reproduction, asserts "required" + blocker, then asserts the progress panel/bars/“Transferring…” during the two-file upload (screen evidence/shared-v1/pdf-queue-progress-1440.jpeg).
- Also answered the review checkbox semantics (allow_partial / publish / confirm) from app pdf_jobs.py L260-320.
## (dated) Owner-executed production workflow — 2026-09-12 follow-up, superseded by the v3 section above where they differ
User authorization update: controlled real SMS/OTP login testing to owner number 9999813334 IS authorized (supersedes isolated-only rule; do not re-ask). Owner controls production via Manage Publishes → Secrets / Database + Republish; Emergent support runs no scripts; new setting names must exist in backend/.env with non-working placeholders; existing production values change only in Secrets; normal Republish preserves DB (never "Replace with a fresh database"). Never ask for another operator; never ask for secret values in chat.
- backend/.env (workspace, gitignored) now registers CANONICAL_API_BASE_URL, ENROLLMENT_INTEGRATION_KEY, STAFF_SERVICE_KEY, BUILD_COMMIT with `PLACEHOLDER_NOT_CONFIGURED*` (all invalid by design: non-URL, <32 chars, non-hex). Preview stays 503; response shows flags false by NAME and no placeholder text.
- config.py: `Settings.presence()` = validity by name; `Settings.commit` accepts only hex 7–40 else 'unrecorded'. readiness.py: declared `credential_readiness` capability without per-flow payload → INCOMPATIBLE (no legacy fallback); `upstream.credential_verification_supported`; credential_verified null = unknown. 7 new tests. Suite: 68 collected = 65 passed + 3 strict XFAIL. Evidence refreshed in evidence/auth-production-incident/readiness-adapter-v2/.
- PRODUCTION_OPERATOR_HANDOFF.md rewritten for owner-executed flow (change states table, app-first order, Secrets checklist, Save-to-GitHub/Republish steps from support, no-SMS checks, live OTP test steps incl. /api/admin/auth/me ID+role check, per-domain sessions, ≤1 resend).
- Git: local platform commit 3384da2 (v2) exists; GitHub main still 8cf6a3c → owner must Save to GitHub; BUILD_COMMIT = that SHA in Secrets before Republish.
- Live recheck: unchanged (app staff key missing; both websites old build + settings missing; hosted origin rejected). SMS requested: NO. SMS received: n/a. Login verified: NO. Reason: prerequisites unmet; website pre-OTP gate is 503, so no SMS attempted.
- Next: after owner applies Secrets + Republish on app then website and app-side role repair is done → re-run read-only checks on both domains → owner performs live OTP login (I cannot type the OTP; owner-executed steps in handoff §D) → verify ID bcdf18c9-dc87-4d46-b580-30cf519103df role admin.