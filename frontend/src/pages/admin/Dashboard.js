import React from 'react';
import {PageTitle,State,useResource,label} from '@/components/admin/SharedUI';
export default function Dashboard(){const r=useResource('/analytics/dashboard',{},true);return <><PageTitle>Operations overview</PageTitle><State resource={r} id="dashboard"/><div className="metrics-strip">{['total_customers','total_users','total_products','total_batches','total_requests','pending_requests','total_executives'].filter(k=>r.data?.[k]!==undefined).map(k=><div key={k}><span>{label(k)}</span><strong data-testid={`dashboard-${k}`}>{r.data[k]}</strong></div>)}</div></>;}